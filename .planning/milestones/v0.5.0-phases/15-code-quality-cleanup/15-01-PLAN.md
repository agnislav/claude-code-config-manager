---
phase: 15-code-quality-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/editCommands.ts
  - src/commands/deleteCommands.ts
  - src/commands/openFileCommands.ts
  - src/extension.ts
  - src/config/configDiscovery.ts
  - src/constants.ts
autonomous: true
requirements:
  - QUAL-01
  - QUAL-02
  - QUAL-03

must_haves:
  truths:
    - "No unused _configStore parameters remain in editCommands, deleteCommands, or openFileCommands"
    - "getAllWatchPaths export no longer exists in configDiscovery.ts"
    - "No unreferenced exports, unused functions, or dead imports remain in src/"
    - "All hardcoded timeout and numeric constants are replaced with named constants in constants.ts"
    - "Each timing constant has a JSDoc comment explaining purpose and rationale"
  artifacts:
    - path: "src/constants.ts"
      provides: "Named timing and numeric constants with JSDoc"
      contains: "EDITOR_SYNC_SUPPRESS_MS"
    - path: "src/commands/editCommands.ts"
      provides: "Edit commands without unused _configStore param"
    - path: "src/commands/deleteCommands.ts"
      provides: "Delete commands without unused _configStore param"
    - path: "src/commands/openFileCommands.ts"
      provides: "Open file commands without unused _configStore param"
    - path: "src/config/configDiscovery.ts"
      provides: "Config discovery without dead getAllWatchPaths export"
  key_links:
    - from: "src/extension.ts"
      to: "registerEditCommands, registerDeleteCommands, registerOpenFileCommands"
      via: "updated call sites with removed configStore argument"
      pattern: "registerEditCommands\\(context\\)"
    - from: "src/extension.ts"
      to: "src/constants.ts"
      via: "imports of new timing constants"
      pattern: "import.*EDITOR_SYNC|TREE_SYNC|DEACTIVATION"
---

<objective>
Remove dead code, unused parameters, and hardcoded magic numbers across the codebase.

Purpose: Clean up technical debt — eliminate unused code paths, remove parameter clutter from command registrations, and make all tunable values discoverable via named constants with documented rationale.

Output: Cleaner command signatures, no dead exports, all magic numbers extracted to constants.ts with JSDoc comments.
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-code-quality-cleanup/15-CONTEXT.md

@src/constants.ts
@src/extension.ts
@src/commands/editCommands.ts
@src/commands/deleteCommands.ts
@src/commands/openFileCommands.ts
@src/config/configDiscovery.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove unused parameters and dead code</name>
  <files>
    src/commands/editCommands.ts
    src/commands/deleteCommands.ts
    src/commands/openFileCommands.ts
    src/extension.ts
    src/config/configDiscovery.ts
  </files>
  <action>
**QUAL-01: Remove unused _configStore parameters:**

1. In `src/commands/editCommands.ts`:
   - Change `registerEditCommands(context: vscode.ExtensionContext, _configStore: ConfigStore)` to `registerEditCommands(context: vscode.ExtensionContext)`
   - Remove the `import { ConfigStore } from '../config/configModel'` since it will no longer be needed

2. In `src/commands/deleteCommands.ts`:
   - Change `registerDeleteCommands(context: vscode.ExtensionContext, _configStore: ConfigStore)` to `registerDeleteCommands(context: vscode.ExtensionContext)`
   - Remove the `import { ConfigStore } from '../config/configModel'` since it will no longer be needed

3. In `src/commands/openFileCommands.ts`:
   - Change `registerOpenFileCommands(context: vscode.ExtensionContext, _configStore: ConfigStore, outputChannel?: vscode.OutputChannel)` to `registerOpenFileCommands(context: vscode.ExtensionContext, outputChannel?: vscode.OutputChannel)`
   - Remove the `import { ConfigStore } from '../config/configModel'` since it will no longer be needed

4. In `src/extension.ts` update the three call sites:
   - `registerEditCommands(context, configStore)` → `registerEditCommands(context)`
   - `registerDeleteCommands(context, configStore)` → `registerDeleteCommands(context)`
   - `registerOpenFileCommands(context, configStore, outputChannel)` → `registerOpenFileCommands(context, outputChannel)`

**QUAL-02: Remove dead code:**

5. In `src/config/configDiscovery.ts`:
   - Delete the entire `getAllWatchPaths` function (lines 63-75) — it is exported but has zero callers anywhere in the codebase

6. Full dead code audit across `src/`:
   - Run a grep for all `export` statements in src/ and verify each has at least one import elsewhere
   - Remove any unreferenced exports, unused helper functions, and dead branches
   - Clean up unused imports in every modified file (use `npm run typecheck` to verify)
   - Exception per user decision: managed scope related code should be COMMENTED OUT (not deleted) with a comment `// TODO: Uncomment when enterprise managed scope support is added`
   - Remove any unused type imports that become orphaned after the above changes
  </action>
  <verify>
    - `npm run typecheck` passes with zero errors
    - `npm run lint` passes
    - Grep for `_configStore` in src/commands/ returns zero results
    - Grep for `getAllWatchPaths` in src/ returns zero results
  </verify>
  <done>
    No unused _configStore parameters remain in any command handler. getAllWatchPaths is deleted. No dead imports or unreferenced exports remain in modified files. Managed scope code is commented out, not deleted.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extract all magic numbers to named constants</name>
  <files>
    src/constants.ts
    src/extension.ts
  </files>
  <action>
**QUAL-03: Extract timeout constants and all magic numbers.**

1. In `src/constants.ts`, add a `// ── Timing Constants ──────────────────────────────────────` section (below existing `// ── Timing ──` section or merge into it). Define these constants with JSDoc:

```typescript
/** Suppress editor→tree sync after tree click to prevent feedback loop. 500ms allows the selection animation to complete. */
export const EDITOR_SYNC_SUPPRESS_MS = 500;

/** Suppress tree→editor sync after programmatic tree reveal. 100ms prevents immediate re-trigger. */
export const TREE_SYNC_SUPPRESS_MS = 100;

/** Debounce delay for editor selection and active editor change events before syncing to tree. 150ms balances responsiveness with avoiding rapid-fire updates. */
export const EDITOR_TREE_SYNC_DEBOUNCE_MS = 150;

/** Polling interval during deactivation to check if in-flight writes are complete. */
export const DEACTIVATION_POLL_INTERVAL_MS = 50;

/** Maximum time to wait for in-flight writes to complete during deactivation. */
export const DEACTIVATION_MAX_WAIT_MS = 5000;
```

Also audit the codebase for any other hardcoded numeric values (retry counts, limits, max depth values):
- `keyPath.length > 10` in openFileCommands.ts — extract to `export const MAX_KEYPATH_DEPTH = 10;` with JSDoc: `/** Maximum allowed keyPath depth for revealInFile validation. */`

2. In `src/extension.ts`, replace all magic numbers:
   - `setTimeout(() => { suppressEditorSync = false; }, 500)` → use `EDITOR_SYNC_SUPPRESS_MS`
   - `setTimeout(() => { suppressTreeSync = false; }, 100)` → use `TREE_SYNC_SUPPRESS_MS`
   - Both `setTimeout(() => { ... }, 150)` for selection and editor sync → use `EDITOR_TREE_SYNC_DEBOUNCE_MS`
   - `const maxWait = 5000;` → use `DEACTIVATION_MAX_WAIT_MS`
   - `const pollInterval = 50;` → use `DEACTIVATION_POLL_INTERVAL_MS`

3. In `src/commands/openFileCommands.ts`:
   - Replace `keyPath.length > 10` with `keyPath.length > MAX_KEYPATH_DEPTH` and import the constant

4. Add the new constants to the imports in each modified file.

Note: DEBOUNCE_RELOAD_MS and DEBOUNCE_MAX_WAIT_MS already exist in constants.ts (added in Phase 12). Keep them, just ensure they are in the same section with consistent JSDoc style.
  </action>
  <verify>
    - `npm run typecheck` passes with zero errors
    - `npm run lint` passes
    - Grep for hardcoded timeout values in extension.ts: `grep -E '\b(50|100|150|500|5000)\b' src/extension.ts` returns only the constant imports, not literal numbers
    - All timing constants in constants.ts have JSDoc comments
  </verify>
  <done>
    All hardcoded timeout and numeric values in extension.ts and openFileCommands.ts are replaced with named constants. constants.ts contains a dedicated timing section with JSDoc-documented constants explaining purpose and rationale.
  </done>
</task>

</tasks>

<verification>
- `npm run compile` succeeds (both typecheck and esbuild bundle)
- `npm run lint` passes with no new warnings
- No _configStore parameters in editCommands.ts, deleteCommands.ts, or openFileCommands.ts
- No getAllWatchPaths export in configDiscovery.ts
- No bare numeric literals for timeouts in extension.ts
- All timing constants have JSDoc comments in constants.ts
</verification>

<success_criteria>
- QUAL-01 complete: unused _configStore parameters removed from all three command modules, call sites in extension.ts updated
- QUAL-02 complete: getAllWatchPaths deleted, full dead code sweep done, no unreferenced exports remain
- QUAL-03 complete: all magic numbers extracted to named constants with JSDoc documentation
- Code compiles, lints, and bundles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-code-quality-cleanup/15-01-SUMMARY.md`
</output>
