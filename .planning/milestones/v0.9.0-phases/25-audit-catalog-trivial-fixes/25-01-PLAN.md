---
phase: 25-audit-catalog-trivial-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-audit-catalog-trivial-fixes/25-AUDIT-MATRIX.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02

must_haves:
  truths:
    - "A complete audit matrix exists covering all node types (12 NodeKind values) across 5 audit vectors"
    - "Each finding is classified as OK, Intentional (with rationale), or Gap (with target phase)"
    - "A gap tracking table maps each gap to its target phase and requirement ID"
  artifacts:
    - path: ".planning/phases/25-audit-catalog-trivial-fixes/25-AUDIT-MATRIX.md"
      provides: "Complete audit matrix with summary table and detailed findings"
      contains: "NodeKind"
  key_links: []
---

<objective>
Create a comprehensive audit matrix documenting actual vs expected behavior for all node types across 5 audit vectors (tooltip, inline buttons, context menu, click behavior, overlap detection).

Purpose: This matrix is the primary deliverable of Phase 25 and feeds Phases 26-28 with classified findings (gaps mapped to requirement IDs). It establishes the baseline understanding of the entire tree UX.
Output: `25-AUDIT-MATRIX.md` in the phase directory
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-audit-catalog-trivial-fixes/25-CONTEXT.md
@.planning/phases/25-audit-catalog-trivial-fixes/25-RESEARCH.md

@src/viewmodel/builder.ts
@src/viewmodel/types.ts
@src/types.ts
@src/constants.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate audit matrix via code analysis</name>
  <files>.planning/phases/25-audit-catalog-trivial-fixes/25-AUDIT-MATRIX.md</files>
  <action>
Analyze the codebase to produce a comprehensive audit matrix. This is a documentation task -- no code changes.

**Sources to analyze:**
1. `src/viewmodel/types.ts` -- enumerate all 12 NodeKind values (WorkspaceFolder, Scope, Section, PermissionRule, Setting, SettingKeyValue, EnvVar, Plugin, McpServer, SandboxProperty, HookEvent, HookEntry)
2. `src/viewmodel/builder.ts` -- for each node type, extract actual behavior for:
   - **Tooltip**: What tooltip is built? MarkdownString or undefined? Base content or overlap-only?
   - **Overlap detection**: Does the node type have an overlap resolution function? Which one?
   - **Click behavior**: Does `computeCommand()` apply (revealInFile) or is it a container (expand/collapse)?
3. `package.json` menus section -- for each node type, extract:
   - **Inline buttons**: What appears in `view/item/inline`? (Currently 0 entries -- document this)
   - **Context menu**: What appears in `view/item/context`? Match `when` clause regex patterns to node contextValue patterns
4. Note any `&& false` disabled guards and which nodes they affect

**Document structure (per CONTEXT.md locked decisions):**

```markdown
# Phase 25: Audit Matrix
## Summary Overview
[Table: Node Type | Tooltip | Inline Buttons | Context Menu | Click | Overlap | Status]
Status = count of OK/Intentional/Gap per row

## Detailed Findings
[Only for node types with Intentional or Gap findings]
For each finding:
- Audit vector
- Current behavior (actual)
- Expected behavior
- Classification: OK / Intentional (with one-line rationale) / Gap
- For Gap: note that tracking table references the target phase

## Gap Tracking Table
[Table: Gap ID | Node Type | Vector | Description | Target Phase | Requirement ID]
Include all gaps found, referencing TRIV-xx, INLN-xx, OVLP-xx, ACTN-xx requirement IDs
```

**Classification rules (per CONTEXT.md):**
- **OK**: Actual matches expected behavior
- **Intentional**: Differs from expected but by design -- include one-line rationale inline in status cell
- **Gap**: Unintentional inconsistency needing fix -- reference target phase in tracking table

**Key findings to expect from RESEARCH.md analysis:**
- EnvVar tooltip: Gap (TRIV-03) -- no base tooltip, overlap-only
- HookEntry description: Gap (TRIV-02) -- empty description (note: description is not an audit vector, but mention in tooltip/context findings if relevant)
- Sandbox section count: Gap (TRIV-01) -- empty description (same note)
- Inline buttons: 0 entries for all types -- Gap for INLN-01 through INLN-04
- HookEntry/HookEvent overlap: Gap for OVLP-01, OVLP-02
- Plugin move/copy/delete disabled: Intentional (per REQUIREMENTS.md out of scope)
- MCP Server missing enriched tooltip details: Gap for ACTN-02
- EnvVar missing copy-to-scope: Gap for ACTN-01
- SettingKeyValue missing edit/delete: Gap for ACTN-04, ACTN-05

Note on "14 node types" from CONTEXT.md: NodeKind enum has 12 values. The CONTEXT.md mentions 14 including containers. Document exactly what exists per NodeKind. If PermissionGroup appears as a conceptual UI grouping (allow/deny/ask headers) but has no distinct NodeKind, note this in the matrix preamble.
  </action>
  <verify>
    <automated>test -f .planning/phases/25-audit-catalog-trivial-fixes/25-AUDIT-MATRIX.md && grep -c "NodeKind\|node type\|Node Type" .planning/phases/25-audit-catalog-trivial-fixes/25-AUDIT-MATRIX.md</automated>
  </verify>
  <done>
- 25-AUDIT-MATRIX.md exists with summary overview table covering all 12 NodeKind values
- Each node type has 5 audit vector assessments (tooltip, inline buttons, context menu, click behavior, overlap)
- Every finding classified as OK, Intentional (with rationale), or Gap
- Gap tracking table maps each gap to target phase and requirement ID
- All v0.9.0 requirement IDs (TRIV-01/02/03, INLN-01/02/03/04, OVLP-01/02, ACTN-01/02/03/04/05) appear in tracking table where applicable
  </done>
</task>

</tasks>

<verification>
- 25-AUDIT-MATRIX.md exists and is well-structured
- All 12 NodeKind values appear in the summary table
- Gap tracking table references requirement IDs from REQUIREMENTS.md
- No code files were modified
</verification>

<success_criteria>
- Complete audit matrix with hybrid structure (summary + detailed findings)
- All findings classified with 3-way status
- Gap tracking table provides clear input for Phases 26-28 planning
</success_criteria>

<output>
After completion, create `.planning/phases/25-audit-catalog-trivial-fixes/25-01-SUMMARY.md`
</output>
