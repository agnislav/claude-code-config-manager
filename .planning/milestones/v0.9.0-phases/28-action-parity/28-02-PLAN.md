---
phase: 28-action-parity
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/utils/platform.ts
  - src/constants.ts
  - src/config/configDiscovery.ts
  - src/config/configModel.ts
  - src/config/configWriter.ts
  - src/config/configLoader.ts
  - src/commands/moveCommands.ts
  - src/viewmodel/builder.ts
  - src/watchers/fileWatcher.ts
  - package.json
autonomous: true
requirements: [ACTN-02, ACTN-03]

must_haves:
  truths:
    - "MCP servers defined in ~/.claude.json top-level mcpServers appear under the User scope in the tree"
    - "MCP servers defined in ~/.claude.json projects[path].mcpServers appear under the Project Local scope in the tree"
    - "MCP servers from .mcp.json continue to appear under the Project Shared scope"
    - "MCP Server nodes show move@1, copy@2, delete@3 inline buttons"
    - "MCP Server tooltip includes scope info line matching EnvVar tooltip pattern"
    - "Copying an MCP server to a different scope writes to the correct file and JSON path"
    - "Writing to ~/.claude.json preserves all non-MCP data in the file"
  artifacts:
    - path: "src/utils/platform.ts"
      provides: "getUserClaudeJsonPath() function"
      exports: ["getUserClaudeJsonPath"]
    - path: "src/config/configDiscovery.ts"
      provides: "Discovery of User and Local MCP entries from ~/.claude.json"
      exports: ["discoverConfigPaths", "DiscoveredPaths"]
    - path: "src/config/configWriter.ts"
      provides: "Scope-aware MCP server writers for ~/.claude.json"
      exports: ["setUserMcpServer", "removeUserMcpServer", "setLocalMcpServer", "removeLocalMcpServer"]
    - path: "src/config/configModel.ts"
      provides: "MCP data attached to User and ProjectLocal ScopedConfig entries"
    - path: "src/commands/moveCommands.ts"
      provides: "copyMcpServerToScope command and moveToScope MCP branch"
    - path: "src/viewmodel/builder.ts"
      provides: "MCP tooltip with scope info line"
    - path: "package.json"
      provides: "MCP move/copy inline button menu entries and copyMcpServerToScope command"
  key_links:
    - from: "src/config/configDiscovery.ts"
      to: "src/config/configModel.ts"
      via: "DiscoveredPaths.userMcpConfig and localMcpConfig fields"
      pattern: "userMcpConfig|localMcpConfig"
    - from: "src/config/configModel.ts"
      to: "src/viewmodel/builder.ts"
      via: "ScopedConfig.mcpConfig on User and ProjectLocal scopes"
      pattern: "mcpConfig"
    - from: "src/commands/moveCommands.ts"
      to: "src/config/configWriter.ts"
      via: "setUserMcpServer/setLocalMcpServer/setMcpServer dispatch based on target scope"
      pattern: "set(User|Local)?McpServer"
    - from: "src/constants.ts"
      to: "src/config/configWriter.ts"
      via: "getAllowedWritePaths includes ~/.claude.json"
      pattern: "getUserClaudeJsonPath"
---

<objective>
Expand MCP server discovery to read User and Local scopes from `~/.claude.json`, add scope-aware writers, wire move/copy/delete inline buttons, and enrich the MCP tooltip with scope info.

Purpose: MCP servers currently only appear under Project Shared (from `.mcp.json`). Claude Code actually stores MCP servers in 3 scopes. This plan makes all three visible, editable, and movable between scopes — matching the action parity of permissions, settings, and env vars.
Output: Multi-scope MCP discovery, scope-aware writers, move/copy commands, enriched tooltip.
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-action-parity/28-CONTEXT.md
@.planning/phases/28-action-parity/28-RESEARCH.md
@.planning/phases/28-action-parity/28-01-SUMMARY.md

@src/utils/platform.ts
@src/constants.ts
@src/config/configDiscovery.ts
@src/config/configModel.ts
@src/config/configWriter.ts
@src/config/configLoader.ts
@src/commands/moveCommands.ts
@src/viewmodel/builder.ts
@src/watchers/fileWatcher.ts
@src/types.ts
@package.json

<interfaces>
<!-- Key types and contracts the executor needs. -->

From src/types.ts:
```typescript
export interface McpServerStdio { command: string; args?: string[]; env?: Record<string, string>; }
export interface McpServerSse { type: 'sse'; url: string; headers?: Record<string, string>; }
export type McpServerConfig = McpServerStdio | McpServerSse;
export interface McpConfig { mcpServers?: Record<string, McpServerConfig>; }

export interface ScopedConfig {
  scope: ConfigScope;
  filePath: string | undefined;
  fileExists: boolean;
  config: ClaudeCodeConfig;
  mcpConfig?: McpConfig;
  mcpFilePath?: string;
  isReadOnly: boolean;
}

export interface DiscoveredPaths {
  workspaceFolder?: vscode.WorkspaceFolder;
  managed: FileInfo;
  user: FileInfo;
  projectShared?: FileInfo;
  projectLocal?: FileInfo;
  mcp?: FileInfo;
}
```

From src/config/configWriter.ts:
```typescript
export function setMcpServer(filePath: string, serverName: string, config: McpServerConfig): void;
export function removeMcpServer(filePath: string, serverName: string): void;
function loadOrCreate<T>(filePath: string): T;
function trackedWrite(filePath: string, writeFn: () => void): void;
```

From src/config/configLoader.ts:
```typescript
export function loadMcpFile(filePath: string): { data?: McpConfig; error?: string };
```

From src/viewmodel/builder.ts:
```typescript
// buildMcpServers reads scopedConfig.mcpConfig?.mcpServers
// buildOverlapTooltip appends overlap info to base tooltip
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MCP multi-scope discovery and model wiring</name>
  <files>src/utils/platform.ts, src/constants.ts, src/config/configDiscovery.ts, src/config/configModel.ts, src/config/configLoader.ts</files>
  <behavior>
    - getUserClaudeJsonPath() returns path to ~/.claude.json
    - DiscoveredPaths includes userMcpConfig and localMcpConfig fields
    - discoverConfigPaths reads ~/.claude.json and extracts top-level mcpServers (User) and projects[workspacePath].mcpServers (Local)
    - buildScopedConfigs attaches mcpConfig and mcpFilePath to User and ProjectLocal ScopedConfig entries
    - getAllowedWritePaths includes ~/.claude.json path
    - fileWatcher watches ~/.claude.json for external changes (if straightforward)
  </behavior>
  <action>
1. In `platform.ts`, add:
   ```typescript
   export function getUserClaudeJsonPath(): string {
     return path.join(os.homedir(), '.claude.json');
   }
   ```

2. In `constants.ts`, add `getUserClaudeJsonPath` import and include its result in `getAllowedWritePaths()`:
   ```typescript
   paths.add(getUserClaudeJsonPath());
   ```

3. In `configDiscovery.ts`:
   - Add `userMcpConfig?: McpConfig` and `localMcpConfig?: McpConfig` to the `DiscoveredPaths` interface.
   - Add `claudeJsonPath?: string` to `DiscoveredPaths` so the model knows the file path.
   - In `discoverConfigPaths()`, read `~/.claude.json` using `readJsonFile<Record<string, unknown>>()`:
     - Extract top-level `mcpServers` as User MCP config.
     - For each workspace folder, extract `projects[folder.uri.fsPath].mcpServers` as Local MCP config.
   - Populate `userMcpConfig` and `localMcpConfig` on the returned `DiscoveredPaths`.
   - Store `claudeJsonPath` for the model to use as `mcpFilePath`.

4. In `configModel.ts`, update `buildScopedConfigs()`:
   - When pushing the User scope: if `discovered.userMcpConfig` has entries, set `mcpConfig: discovered.userMcpConfig` and `mcpFilePath: discovered.claudeJsonPath`.
   - When pushing the ProjectLocal scope: if `discovered.localMcpConfig` has entries, set `mcpConfig: discovered.localMcpConfig` and `mcpFilePath: discovered.claudeJsonPath`.
   - The existing builder code in `buildMcpServers()` already reads `scopedConfig.mcpConfig?.mcpServers` — once data is attached, MCP servers will automatically render under User and ProjectLocal scopes.

5. In `fileWatcher.ts`: add `getUserClaudeJsonPath()` to the watched paths set so external changes to `~/.claude.json` trigger tree refresh.

PITFALL AVOIDANCE:
- Read `~/.claude.json` as `Record<string, unknown>` — never narrow to `McpConfig` (would lose non-MCP fields on write).
- Use `folder.uri.fsPath` for the project key lookup — matches how Claude Code CLI writes it.
- Set `mcpFilePath` to `claudeJsonPath` (not the settings file path) so `revealInFile` opens the correct file.
  </action>
  <verify>
    <automated>npm run compile</automated>
  </verify>
  <done>MCP servers from ~/.claude.json appear under User and ProjectLocal scopes in the tree. getAllowedWritePaths includes ~/.claude.json. File watcher covers ~/.claude.json.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: MCP scope-aware writers, move/copy commands, tooltip, and package.json wiring</name>
  <files>src/config/configWriter.ts, src/commands/moveCommands.ts, src/viewmodel/builder.ts, src/constants.ts, package.json</files>
  <behavior>
    - setUserMcpServer writes to ~/.claude.json top-level mcpServers without losing other data
    - removeUserMcpServer removes from ~/.claude.json top-level mcpServers without losing other data
    - setLocalMcpServer writes to ~/.claude.json projects[path].mcpServers without losing other data
    - removeLocalMcpServer removes from ~/.claude.json projects[path].mcpServers without losing other data
    - copyMcpServerToScope copies an MCP server config to the correct file/path for the target scope
    - moveToScope handler supports mcpServers rootKey using scope-aware writers
    - MCP tooltip shows "Defined in: {ScopeLabel} ({shortPath})" line
  </behavior>
  <action>
1. In `configWriter.ts`, add four new exported functions for `~/.claude.json` MCP writes. ALL must use `loadOrCreate<Record<string, unknown>>` to preserve non-MCP data:
   - `setUserMcpServer(claudeJsonPath, serverName, config)`: reads full file, ensures `data.mcpServers` exists as object, sets `data.mcpServers[serverName] = config`, writes full object via `trackedWrite`.
   - `removeUserMcpServer(claudeJsonPath, serverName)`: reads full file, deletes `data.mcpServers[serverName]`, writes full object.
   - `setLocalMcpServer(claudeJsonPath, projectPath, serverName, config)`: reads full file, ensures `data.projects[projectPath].mcpServers` exists (creating intermediate objects), sets `[serverName] = config`, writes full object.
   - `removeLocalMcpServer(claudeJsonPath, projectPath, serverName)`: reads full file, deletes from `data.projects[projectPath].mcpServers[serverName]`, writes full object.

2. In `moveCommands.ts`:
   - Add `copyMcpServerToScope` command handler following the `copySettingToScope` pattern:
     - Guard: block Managed scope.
     - Validate keyPath: `keyPath[0] === 'mcpServers' && keyPath.length === 2`.
     - Extract `serverName = keyPath[1]` and the server config from source `scopedConfig.mcpConfig.mcpServers[serverName]`.
     - Build targetScopes: exclude current, Managed, locked.
     - Show QuickPick with scope labels.
     - Check target for duplicate server name — ask overwrite if exists.
     - Dispatch write based on target scope:
       - User → `setUserMcpServer(getUserClaudeJsonPath(), serverName, config)`
       - ProjectLocal → `setLocalMcpServer(getUserClaudeJsonPath(), workspacePath, serverName, config)`
       - ProjectShared → `setMcpServer(mcpJsonPath, serverName, config)` (existing writer)
     - Show success toast.
   - Extend `moveToScope` handler to include `mcpServers` rootKey branch:
     - Delete from source using scope-aware remove function (same dispatch logic as copy but with remove).
     - Write to target using scope-aware set function.
   - Register both commands.

3. In `builder.ts`, update `buildMcpServers()` to add scope info line to tooltip:
   - After building the existing base tooltip (type + command/URL), append:
     ```
     \n\nDefined in: ${SCOPE_LABELS[scopedConfig.scope]} (${shortPath})
     ```
   - Use `scopedConfig.mcpFilePath ?? scopedConfig.filePath` for the short path (already the pattern used for `revealInFile`).
   - The overlap tooltip section is appended after by `buildOverlapTooltip()` — scope line goes between base info and overlap.

4. In `constants.ts`, add MESSAGES entries:
   - `copiedMcpServer: (name: string, scope: string) => \`Copied MCP server "${name}" to ${scope}\``
   - `movedMcpServer: (name: string, scope: string) => \`Moved MCP server "${name}" to ${scope}\``

5. In `package.json`:
   - Add command declaration: `{ "command": "claudeConfig.copyMcpServerToScope", "title": "Copy to Scope...", "category": "Claude Config", "icon": "$(add)" }`
   - Add inline menu entries:
     - `moveToScope` inline@1 for `^mcpServer\\.editable`: `{ "command": "claudeConfig.moveToScope", "when": "view == claudeConfigTree && viewItem =~ /^mcpServer\\.editable/", "group": "inline@1" }`
     - `copyMcpServerToScope` inline@2 for `^mcpServer\\.editable`: `{ "command": "claudeConfig.copyMcpServerToScope", "when": "view == claudeConfigTree && viewItem =~ /^mcpServer\\.editable/", "group": "inline@2" }`
   - Add context menu entries in `view/item/context` for MCP copy and move (same group as other copy/move commands).
   - Verify `deleteItem` inline@3 regex already covers `mcpServer` — it should per existing wiring.
   - Extend `moveToScope` context menu when clause to include `mcpServer` if not already present.

6. Update REQUIREMENTS.md: Remove the "MCP Server move/copy between scopes" row from the Out of Scope table (it was based on the wrong assumption that MCP is workspace-only, per CONTEXT.md specifics).
  </action>
  <verify>
    <automated>npm run compile && npm run lint</automated>
  </verify>
  <done>MCP servers have move@1, copy@2, delete@3 inline buttons. Copying/moving dispatches to the correct scope-aware writer. MCP tooltip shows scope info line. Writing to ~/.claude.json preserves all non-MCP data.</done>
</task>

</tasks>

<verification>
- `npm run compile` passes with no type errors
- `npm run lint` passes
- MCP servers appear under User, ProjectLocal, and ProjectShared scopes when configured
- MCP inline buttons show move@1, copy@2, delete@3
- MCP tooltip includes "Defined in:" scope line
- ~/.claude.json writes preserve non-MCP fields (critical safety check)
</verification>

<success_criteria>
- MCP servers from all 3 scopes are visible in the tree
- Copy/move between MCP scopes writes to the correct file and JSON path
- MCP tooltip shows server type, command/URL details, and scope info
- No data loss when writing to ~/.claude.json (preserves all non-MCP content)
</success_criteria>

<output>
After completion, create `.planning/phases/28-action-parity/28-02-SUMMARY.md`
</output>
