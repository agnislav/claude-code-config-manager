---
phase: 26-inline-button-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .planning/phases/25-audit-catalog-trivial-fixes/25-AUDIT-MATRIX.md
autonomous: true
requirements:
  - INLN-03
  - INLN-04

must_haves:
  truths:
    - "Dead editValue guard for envVar/sandboxProperty is removed from package.json"
    - "Plugin && false guards remain intact in package.json (intentional design)"
    - "EnvVar moveToScope renders at inline@1 (not inline@0)"
    - "Setting moveToScope renders at inline@1 (not inline@0)"
    - "Setting copySettingToScope renders at inline@2 (not inline@1)"
    - "deleteItem remains at inline@3 for all entity types (unchanged)"
  artifacts:
    - path: "package.json"
      provides: "Corrected inline button slot positions and dead guard removal"
      contains: "inline@1.*envVar"
    - path: ".planning/phases/25-audit-catalog-trivial-fixes/25-AUDIT-MATRIX.md"
      provides: "Formal documentation of intentional plugin guard design decisions"
      contains: "Design Decision"
  key_links:
    - from: "package.json menus view/item/context"
      to: "ITEMS.md inline button spec"
      via: "group attribute values match ITEMS.md positions"
      pattern: "inline@[0-3]"
---

<objective>
Remove the dead `editValue && false` guard from package.json, reposition three inline button entries to match ITEMS.md slot convention (edit@0, move@1, copy@2, delete@3), and formally document the intentional plugin `&& false` guards in the Phase 25 audit matrix.

Purpose: Achieve uniform inline button ordering across all entity types and clean up dead manifest entries that cause confusion during audits.
Output: Updated package.json with correct inline button positions; updated audit matrix with plugin guard design decision documentation.
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ITEMS.md
@.planning/phases/26-inline-button-cleanup/26-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead guard and reposition inline button slots in package.json</name>
  <files>package.json</files>
  <action>
In `package.json` under `contributes.menus."view/item/context"`, make exactly these changes:

1. **DELETE** the dead `editValue` entry block (currently around lines 300-303):
   ```json
   {
     "command": "claudeConfig.editValue",
     "when": "view == claudeConfigTree && viewItem =~ /\\.editable/ && viewItem =~ /envVar|sandboxProperty/ && false",
     "group": "inline@1"
   }
   ```
   Remove the entire object and fix the surrounding comma to maintain valid JSON.

2. **UPDATE** the `moveToScope` entry for envVar (currently around line 306-309):
   Change `"group": "inline@0"` to `"group": "inline@1"`
   (The `when` clause `viewItem =~ /^envVar\\.editable/` is unchanged.)

3. **UPDATE** the `moveToScope` entry for setting (currently around line 330-333):
   Change `"group": "inline@0"` to `"group": "inline@1"`
   (The `when` clause `viewItem =~ /^setting\\.editable/` is unchanged.)

4. **UPDATE** the `copySettingToScope` entry for setting (currently around line 335-338):
   Change `"group": "inline@1"` to `"group": "inline@2"`
   (The `when` clause `viewItem =~ /^setting\\.editable/` is unchanged.)

DO NOT touch:
- The three plugin `&& false` guard entries (lines 286-298) -- these are intentional
- The `deleteItem` entry at `inline@3` (already correct)
- The `changePermissionType` entry at `inline@0` (already correct)
- The `copyPermissionToScope` entry at `inline@2` (already correct)
- Any `command` or `when` clause values -- only `group` values change

After editing, verify the JSON is valid by running `npm run compile`.
  </action>
  <verify>
    <automated>cd /Users/agnislav/Projects/Dardes/claude-code-config-manager && npm run compile</automated>
  </verify>
  <done>
- The `editValue` entry with `&& false` targeting `envVar|sandboxProperty` no longer exists in package.json
- EnvVar `moveToScope` is at `inline@1`
- Setting `moveToScope` is at `inline@1`
- Setting `copySettingToScope` is at `inline@2`
- Plugin `&& false` guards for move/copy/delete remain intact at inline@1, inline@2, inline@3
- `npm run compile` passes with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Document plugin guard design decisions in audit matrix</name>
  <files>.planning/phases/25-audit-catalog-trivial-fixes/25-AUDIT-MATRIX.md</files>
  <action>
The audit matrix already notes the plugin inline buttons as "Intentional" (under the Plugin section, finding 1). Enrich this documentation with a formal design decision block.

After the existing Plugin finding (lines 65-68 area), add or update the classification to include:

```
- **Design Decision (Phase 26 — INLN-03):** The three `&& false` guards on plugin inline buttons (`moveToScope`, `copyPluginToScope`, `deletePlugin`) are confirmed intentional and must be preserved. Rationale:
  - Plugin interaction uses a checkbox toggle model (Phase 23)
  - Move/copy/delete as inline buttons conflicts with checkbox UX
  - Copy is available via context menu for locked scopes
  - These are tracked as DEFR-01 in REQUIREMENTS.md and listed as Out of Scope
  - The `&& false` guards serve as both suppression and documentation of the design decision
```

Also update the `editValue` guard entry in the "Disabled Inline Buttons" table (around line 182) to note it was removed in Phase 26:

Change the status from `Gap -- INLN-01, INLN-02` to `Removed in Phase 26 (INLN-03) -- editValue for envVar/sandboxProperty deferred to EditValue phase (DEFR-06, DEFR-07)`
  </action>
  <verify>
    <automated>cd /Users/agnislav/Projects/Dardes/claude-code-config-manager && grep -c "Design Decision (Phase 26" .planning/phases/25-audit-catalog-trivial-fixes/25-AUDIT-MATRIX.md</automated>
  </verify>
  <done>
- Plugin section in audit matrix contains formal design decision documentation referencing INLN-03, DEFR-01, and Phase 23
- Dead editValue guard entry is updated to reflect its removal in Phase 26
  </done>
</task>

</tasks>

<verification>
1. `npm run compile` passes (JSON validity + TypeScript compilation)
2. `npm run lint` passes (no lint regressions)
3. In package.json `view/item/context` section:
   - No entry with `editValue` and `&& false` targeting `envVar|sandboxProperty`
   - Plugin `&& false` guards still present (3 entries)
   - EnvVar moveToScope at `inline@1`
   - Setting moveToScope at `inline@1`
   - Setting copySettingToScope at `inline@2`
   - deleteItem at `inline@3` (unchanged)
4. Audit matrix has plugin guard design decision documentation
</verification>

<success_criteria>
- package.json inline button positions match ITEMS.md for all entity types
- Dead guard removed, intentional guards preserved and documented
- Build compiles cleanly with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/26-inline-button-cleanup/26-01-SUMMARY.md`
</output>
