---
phase: 05-add-move-inline-button
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/commands/moveCommands.ts
  - package.json
autonomous: true
requirements:
  - MOVE-01
  - MOVE-02
  - MOVE-03

must_haves:
  truths:
    - "Tree items with a copy inline icon button also show a move inline icon button to the left of copy"
    - "Clicking the move inline button on a permissionRule, setting, or plugin shows a confirmation dialog, then opens the move-to-scope picker"
    - "Move for plugins writes the plugin entry to the target scope and removes it from the source scope"
    - "Move inline button only appears on .editable items, not read-only scope items"
  artifacts:
    - path: "src/commands/moveCommands.ts"
      provides: "Extended moveToScope with plugin support and confirmation dialog"
      contains: "enabledPlugins"
    - path: "package.json"
      provides: "Move inline button menu contributions with when clauses matching copy patterns"
      contains: "arrow-both"
  key_links:
    - from: "package.json inline menu entries"
      to: "claudeConfig.moveToScope command"
      via: "command string in view/item/context inline entries"
      pattern: "claudeConfig\\.moveToScope.*inline"
    - from: "moveToScope handler"
      to: "configWriter.removePlugin + configWriter.setPluginEnabled"
      via: "plugin branch in moveToScope handler"
      pattern: "enabledPlugins.*setPluginEnabled.*removePlugin"
---

<objective>
Add move inline icon button alongside existing copy inline icon button on permissionRule, plugin, and setting tree items. Extend the existing moveToScope command to support plugins and add a confirmation dialog before destructive move operations.

Purpose: Provides one-click move action from the tree item inline buttons, matching the existing copy button pattern. Move is destructive (removes from source), so a confirmation dialog prevents accidental moves.

Output: Updated moveCommands.ts with plugin support and confirmation; updated package.json with move inline entries and adjusted button ordering.
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-add-move-inline-button/05-CONTEXT.md
@src/commands/moveCommands.ts
@src/commands/pluginCommands.ts
@src/config/configWriter.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend moveToScope with plugin support and confirmation dialog</name>
  <files>src/commands/moveCommands.ts</files>
  <action>
Modify the existing `claudeConfig.moveToScope` handler in `src/commands/moveCommands.ts` to:

1. **Add confirmation dialog** — Before opening the scope picker, show a modal warning message:
   ```typescript
   const confirmMove = await vscode.window.showWarningMessage(
     `Move "${node.label}" to a different scope? This will remove it from ${SCOPE_LABELS[scope]}.`,
     { modal: true },
     'Move',
   );
   if (confirmMove !== 'Move') return;
   ```
   Insert this AFTER the read-only/lock guard but BEFORE the target scope picker. Per user decision, move is destructive so an extra confirmation step prevents accidental moves. This differs from the copy flow which goes straight to the picker.

2. **Add plugin branch** — After the existing `env` branch (keyPath[0] === 'env') and before the scalar setting fallback, add a plugin branch:
   ```typescript
   } else if (rootKey === 'enabledPlugins' && keyPath.length === 2) {
     const pluginId = keyPath[1];
     const currentSc = allScopes.find((s) => s.scope === scope);
     const enabled = currentSc?.config.enabledPlugins?.[pluginId] ?? true;
     setPluginEnabled(targetFilePath, pluginId, enabled);
     removePlugin(filePath, pluginId);
   ```
   Import `setPluginEnabled` and `removePlugin` from `../config/configWriter` at the top of the file (they already exist in configWriter.ts — same functions used by pluginCommands.ts).

3. **Update the existing imports** — Add `setPluginEnabled` and `removePlugin` to the import from `../config/configWriter`:
   ```typescript
   import {
     addPermissionRule,
     removePermissionRule,
     setEnvVar,
     removeEnvVar,
     setScalarSetting,
     removeScalarSetting,
     setPluginEnabled,
     removePlugin,
   } from '../config/configWriter';
   ```

Lock-aware behavior: The existing source guard already blocks move from locked User scope (lines 28-34). The existing target picker already filters out locked scopes (line 49). No additional lock logic needed.
  </action>
  <verify>
Run `npm run typecheck` — must pass with no errors. Verify that the moveToScope handler now has three explicit branches: permissions (keyPath.length === 3), env (keyPath.length === 2), enabledPlugins (keyPath.length === 2), and scalar setting fallback.
  </verify>
  <done>
moveToScope command handles plugins by writing to target scope via setPluginEnabled and removing from source via removePlugin. Confirmation dialog appears before scope picker for all move operations.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add move inline button entries and adjust button ordering in package.json</name>
  <files>package.json</files>
  <action>
Modify `package.json` to add the move inline icon button and adjust existing inline button ordering.

1. **Add icon to moveToScope command definition** — In the `commands` array, find the `claudeConfig.moveToScope` entry and add the icon:
   ```json
   {
     "command": "claudeConfig.moveToScope",
     "title": "Move to Scope...",
     "category": "Claude Config",
     "icon": "$(arrow-both)"
   }
   ```
   Per user decision: use `$(arrow-both)` icon (vertical double-headed arrow) to contrast with `$(copy)`.

2. **Update the context menu `when` clause for moveToScope** — In the `view/item/context` section, find the existing moveToScope context menu entry:
   ```json
   {
     "command": "claudeConfig.moveToScope",
     "when": "view == claudeConfigTree && viewItem =~ /\\.editable/ && viewItem =~ /permissionRule|envVar|setting/",
     "group": "2_move"
   }
   ```
   Change the `when` clause to include `plugin`:
   ```json
   "when": "view == claudeConfigTree && viewItem =~ /\\.editable/ && viewItem =~ /permissionRule|envVar|plugin|setting/"
   ```

3. **Add three move inline button entries** — In the `view/item/context` array, add entries for the move inline button on permissionRule, plugin, and setting. These match the SAME `when` patterns as the corresponding copy inline entries, but point to `claudeConfig.moveToScope`:

   For permissionRule (move appears at inline@0, before copy@1):
   ```json
   {
     "command": "claudeConfig.moveToScope",
     "when": "view == claudeConfigTree && viewItem =~ /^permissionRule\\.editable/",
     "group": "inline@0"
   }
   ```

   For setting (move appears at inline@0, before copy@1):
   ```json
   {
     "command": "claudeConfig.moveToScope",
     "when": "view == claudeConfigTree && viewItem =~ /^setting\\.editable/",
     "group": "inline@0"
   }
   ```

   For plugin (move appears at inline@1, after readme@0, before copy):
   ```json
   {
     "command": "claudeConfig.moveToScope",
     "when": "view == claudeConfigTree && viewItem =~ /^plugin\\.editable/",
     "group": "inline@1"
   }
   ```

4. **Adjust existing inline@ numbers to maintain desired ordering:**

   For **plugin** items, the desired layout is: `[readme@0]` `[move@1]` `[copy@2]` `[delete@3]`.
   - `openPluginReadme` stays at `inline@0` (no change)
   - Move is `inline@1` (new entry above)
   - `copyPluginToScope` changes from `inline@1` to `inline@2`
   - `deletePlugin` changes from `inline@2` to `inline@3`

   For **permissionRule** items, the desired layout is: `[move@0]` `[copy@1]` `[delete@2]`.
   - Move is `inline@0` (new entry above)
   - `copyPermissionToScope` stays at `inline@1` (no change needed)
   - `deleteItem` for permissionRule stays at `inline@2` (no change — the generic deleteItem inline@2 entry already covers it)

   For **setting** items, the desired layout is: `[move@0]` `[copy@1]` `[delete@2]`.
   - Move is `inline@0` (new entry above)
   - `copySettingToScope` stays at `inline@1` (no change needed)
   - `deleteItem` for setting stays at `inline@2` (no change — the generic deleteItem inline@2 entry already covers it)

   So only the plugin entries need renumbering:
   - Change `copyPluginToScope` from `"group": "inline@1"` to `"group": "inline@2"`
   - Change `deletePlugin` from `"group": "inline@2"` to `"group": "inline@3"`

5. **No commandPalette changes needed** — `moveToScope` is already in the commandPalette deny-list (`"when": "false"` on line 297-299). The same command is reused for the inline button, so it's already hidden from the palette.
  </action>
  <verify>
Run `npm run typecheck` to verify no issues. Visually inspect `package.json` to confirm:
- moveToScope has `$(arrow-both)` icon
- Three new inline entries for moveToScope (permissionRule@0, setting@0, plugin@1)
- copyPluginToScope is inline@2, deletePlugin is inline@3
- moveToScope context menu `when` clause includes `plugin`
- No duplicate or conflicting inline@ numbers within the same node type
  </verify>
  <done>
package.json has move inline icon button entries for permissionRule, plugin, and setting. Button ordering is: move before copy before delete (with readme first for plugins). The `$(arrow-both)` icon is used per user decision.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with zero errors
2. `npm run compile` succeeds (type-check + esbuild bundle)
3. In package.json: moveToScope command has `"icon": "$(arrow-both)"`
4. In package.json: three new `view/item/context` inline entries for `claudeConfig.moveToScope` targeting permissionRule, plugin, setting
5. In package.json: plugin inline buttons are ordered readme@0, move@1, copy@2, delete@3
6. In package.json: permissionRule and setting inline buttons are ordered move@0, copy@1, delete@2
7. In moveCommands.ts: moveToScope handler has an `enabledPlugins` branch that calls `setPluginEnabled` + `removePlugin`
8. In moveCommands.ts: confirmation dialog appears before the scope picker
9. In package.json: moveToScope context menu `when` clause includes `plugin` alongside `permissionRule|envVar|setting`
</verification>

<success_criteria>
- Tree items that show a copy inline icon button also show a move inline icon button to the left of copy (MOVE-01)
- Clicking the move inline button invokes the existing moveToScope command with confirmation dialog then scope picker (MOVE-02)
- package.json menu contributions include move icon entries with when clauses matching the same contextValue patterns as copy entries (MOVE-03)
- Move button uses `$(arrow-both)` icon per user decision
- Move button only visible on `.editable` items per user decision
- Plugin move works end-to-end: plugin entry written to target scope, removed from source scope
</success_criteria>

<output>
After completion, create `.planning/phases/05-add-move-inline-button/05-01-SUMMARY.md`
</output>
