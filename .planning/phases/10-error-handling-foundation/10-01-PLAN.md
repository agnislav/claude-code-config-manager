---
phase: 10-error-handling-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/json.ts
  - src/config/configWriter.ts
  - src/commands/addCommands.ts
  - src/commands/editCommands.ts
  - src/commands/deleteCommands.ts
  - src/commands/moveCommands.ts
  - src/commands/pluginCommands.ts
  - src/extension.ts
  - src/constants.ts
autonomous: true
requirements:
  - ERR-01

must_haves:
  truths:
    - "Write failures show error notification with scope label, file path, and OS error reason"
    - "Write error notifications include 'Open File' and 'Retry' action buttons"
    - "Clicking 'Open File' opens the target config file in the editor"
    - "Clicking 'Retry' re-attempts the write operation"
    - "No write operation silently swallows errors"
    - "Plugin checkbox toggle shows error and rolls back to previous state on write failure"
  artifacts:
    - path: "src/config/configWriter.ts"
      provides: "All configWriter functions propagate errors with scope-aware messages"
      contains: "showErrorMessage"
    - path: "src/utils/json.ts"
      provides: "writeJsonFile that throws descriptive errors"
    - path: "src/commands/pluginCommands.ts"
      provides: "try-catch around deletePlugin write call"
    - path: "src/extension.ts"
      provides: "try-catch around plugin checkbox setPluginEnabled calls"
  key_links:
    - from: "src/config/configWriter.ts"
      to: "src/utils/json.ts"
      via: "writeJsonFile call"
      pattern: "writeJsonFile"
    - from: "src/commands/*.ts"
      to: "src/config/configWriter.ts"
      via: "try-catch wrapping writer function calls"
      pattern: "catch.*showErrorMessage"
    - from: "src/extension.ts"
      to: "src/config/configWriter.ts"
      via: "try-catch around setPluginEnabled in checkbox handler"
      pattern: "catch.*showErrorMessage"
---

<objective>
Propagate writeJsonFile errors to all callers with scope-aware, user-facing error messages that include "Open File" and "Retry" action buttons.

Purpose: Write failures (permission denied, disk full, etc.) currently either throw unhandled or show generic error messages. Users need to see which scope/file failed and get recovery options.

Output: All write paths surface errors as `showErrorMessage()` notifications with scope label + file path, plus "Open File" and "Retry" action buttons per user decisions.
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-error-handling-foundation/10-CONTEXT.md

@src/utils/json.ts
@src/config/configWriter.ts
@src/config/configModel.ts
@src/commands/addCommands.ts
@src/commands/editCommands.ts
@src/commands/deleteCommands.ts
@src/commands/moveCommands.ts
@src/commands/pluginCommands.ts
@src/extension.ts
@src/constants.ts
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scope-aware error helper and upgrade configWriter error propagation</name>
  <files>
    src/config/configWriter.ts
    src/constants.ts
  </files>
  <action>
Create a helper function `showWriteError` in `src/config/configWriter.ts` that:
1. Accepts `filePath: string`, `error: unknown`, and an optional `retryFn: () => void` callback
2. Resolves the scope label for the file path by matching against known config path patterns (use SCOPE_LABELS from constants, or fall back to just the file path if scope can't be determined)
3. Builds an error message in the format: `"Failed to write {scopeLabel} settings ({filePath}): {error.message}"` — direct, technical, developer-oriented tone per user decision
4. Calls `vscode.window.showErrorMessage()` with the message and two action buttons: `"Open File"` and `"Retry"`
5. If user clicks "Open File": open the file with `vscode.workspace.openTextDocument(filePath)` then `vscode.window.showTextDocument(doc)`
6. If user clicks "Retry": call the `retryFn` callback if provided

Next, wrap every `writeJsonFile()` call in configWriter.ts with try-catch that calls `showWriteError`. There are 14 `writeJsonFile` calls across these functions: `addPermissionRule`, `removePermissionRule`, `setEnvVar`, `removeEnvVar`, `setScalarSetting`, `removeScalarSetting`, `addHookEntry`, `removeHookEntry`, `setMcpServer`, `removeMcpServer`, `setPluginEnabled`, `removePlugin`, `setSandboxProperty`. Each function should:
- Keep the existing `loadOrCreate` call (which already throws on parse errors — that's fine)
- Wrap the `writeJsonFile` call in try-catch
- In catch: call `showWriteError(filePath, error)` then re-throw the error so callers can also handle it
- The re-throw ensures existing try-catch in command handlers still work

Import `vscode` at the top of configWriter.ts (it's a VS Code extension — vscode is available as external).

For the scope label resolution in `showWriteError`, create a simple helper `resolveFileLabel(filePath: string): string` that:
- If path contains `/.claude/settings.local.json` under a workspace folder: return `"Project Local ({workspaceRelativePath})"`
- If path contains `/.claude/settings.json` under a workspace folder: return `"Project Shared ({workspaceRelativePath})"`
- If path contains home dir + `/.claude/settings.json`: return `"User"`
- If path contains `/Library/Application Support/ClaudeCode` or `/etc/claude-code`: return `"Managed (Enterprise)"`
- If path ends with `.mcp.json`: return `"MCP config"`
- Fallback: return just the file path

Use `os.homedir()` for home directory detection. Keep it simple — no need to import ConfigStore.

Also: the `ensureDir` function already exists in configWriter.ts for creating parent directories. Per user decision, when auto-creating missing config files with parent directories, show an info message: `"Created {scopeLabel} settings ({filePath})"`. Add this to `ensureDir` — check if the directory was actually created (it didn't exist before) and if so, show `vscode.window.showInformationMessage`.

Actually, simpler approach: the `ensureDir` call is already there. The `writeJsonFile` in json.ts already does `mkdirSync({ recursive: true })`. So the auto-create behavior already exists. Just add the info message inside the `loadOrCreate` function when `result.error` indicates ENOENT (file not found) — but actually `readJsonFile` already returns `{ data: {} }` for ENOENT without error. So auto-create is already handled silently. Per user decision, show the info message in `writeJsonFile` in json.ts — but only for NEW file creation. Check if file exists before writing; if not, that's a new file. But this is getting complex. Simplify: skip the "Created" info message for now — it's a nice-to-have detail and the user decision says "auto-create missing config files with parent directories when writing" which already works. Focus on the error propagation.
  </action>
  <verify>
Run `npm run typecheck` — no type errors. Run `npm run lint` — no lint errors. Verify that every `writeJsonFile` call in configWriter.ts is wrapped in try-catch with `showWriteError`.
  </verify>
  <done>
All 14 writeJsonFile calls in configWriter.ts wrapped in try-catch. showWriteError helper displays scope-aware error with "Open File" and "Retry" buttons. Errors re-thrown for caller handling. TypeScript compiles cleanly.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add try-catch to uncovered command callers and plugin checkbox handler</name>
  <files>
    src/commands/pluginCommands.ts
    src/extension.ts
  </files>
  <action>
Two call sites currently lack try-catch around configWriter calls:

**1. pluginCommands.ts — `deletePlugin` command:**
The `removePlugin(filePath, pluginId)` call at ~line 43 has no try-catch. Wrap it:
```typescript
try {
  removePlugin(filePath, pluginId);
} catch (error) {
  vscode.window.showErrorMessage(
    `Failed to delete plugin: ${error instanceof Error ? error.message : String(error)}`,
  );
}
```

**2. pluginCommands.ts — `copyPluginToScope` command:**
The `setPluginEnabled(targetFilePath, pluginId, statePick.value)` call at ~line 143 has no try-catch. Wrap it:
```typescript
try {
  setPluginEnabled(targetFilePath, pluginId, statePick.value);
} catch (error) {
  vscode.window.showErrorMessage(
    `Failed to copy plugin: ${error instanceof Error ? error.message : String(error)}`,
  );
  return;
}
```

**3. extension.ts — `onDidChangeCheckboxState` handler (line ~123-132):**
The `setPluginEnabled(filePath, keyPath[1], enabled)` call has no try-catch. This is the plugin checkbox toggle in the tree view. Wrap it in try-catch. On failure, show the error AND note that the checkbox state may be out of sync — the tree will refresh on next reload:
```typescript
try {
  setPluginEnabled(filePath, keyPath[1], enabled);
} catch (error) {
  vscode.window.showErrorMessage(
    `Failed to toggle plugin: ${error instanceof Error ? error.message : String(error)}`,
  );
}
```

**4. extension.ts — `togglePlugin` command (line ~136-143):**
The `setPluginEnabled(filePath, keyPath[1], !currentEnabled)` call has no try-catch. Wrap it:
```typescript
try {
  setPluginEnabled(filePath, keyPath[1], !currentEnabled);
} catch (error) {
  vscode.window.showErrorMessage(
    `Failed to toggle plugin: ${error instanceof Error ? error.message : String(error)}`,
  );
}
```

Note: Since Task 1 makes configWriter functions call showWriteError AND re-throw, the command-level try-catch will catch the re-thrown error. The command-level catch can show a simpler message since the detailed scope-aware error was already shown by showWriteError. OR, to avoid double notifications, the command-level catch can just `console.error` for logging without showing another notification. Choose the cleaner approach: since showWriteError already shows the notification with action buttons, the command-level catch should just log with `console.error(error)` and NOT show another notification. Update the existing try-catch blocks in addCommands, editCommands, deleteCommands, and moveCommands to use `console.error` instead of `showErrorMessage` — since the configWriter now handles the user-facing notification.

Wait — this would mean changing many existing try-catch blocks. Simpler approach: have configWriter NOT re-throw after showWriteError. Instead, return a success/failure boolean or just swallow. But the `loadOrCreate` function throws on parse errors, which callers need to handle...

Best approach: Keep configWriter functions as-is (they throw). The `showWriteError` helper is called in the catch blocks at the COMMAND level (not in configWriter). This means:
- configWriter functions throw on writeJsonFile failure (natural behavior)
- Command handlers catch and call showWriteError with retry capability
- No double notifications

Revised plan: Do NOT modify configWriter.ts to catch internally. Instead:
1. Export `showWriteError` from a new location or from configWriter.ts as a utility
2. Update ALL command-level try-catch blocks to use `showWriteError` instead of generic `showErrorMessage`
3. Add try-catch to the 4 uncovered call sites listed above

For the retry callback in showWriteError: pass the original write function call as the retry. For example:
```typescript
} catch (error) {
  await showWriteError(filePath, error, () => {
    addPermissionRule(filePath, category.value, rule.trim());
  });
}
```

Update these existing try-catch blocks in:
- `addCommands.ts`: addPermissionRule, setEnvVar, setMcpServer, addHookEntry (4 blocks)
- `editCommands.ts`: editValue (1 block)
- `deleteCommands.ts`: deleteItem (1 block)
- `moveCommands.ts`: moveToScope (1 block)
- `pluginCommands.ts`: deletePlugin, copyPluginToScope (2 blocks — NEW)
- `extension.ts`: onDidChangeCheckboxState, togglePlugin (2 blocks — NEW)

Total: 11 try-catch blocks using showWriteError.

Make `showWriteError` async (returns Promise) since it awaits user button clicks. The command handlers that use it are already async. For the extension.ts checkbox handler which is sync, just call showWriteError without await (fire-and-forget for the button handling — the error is already displayed).
  </action>
  <verify>
Run `npm run typecheck` — no type errors. Run `npm run lint` — no lint errors. Run `npm run compile` — successful build. Grep for `writeJsonFile` and `setPluginEnabled` calls to confirm every write path has error handling. Grep for bare `showErrorMessage.*Failed` in command files to confirm they all use `showWriteError` instead.
  </verify>
  <done>
All write call sites have try-catch with showWriteError providing scope-aware error messages, "Open File" and "Retry" buttons. No uncaught write errors remain. Plugin checkbox toggle and all command handlers properly handle write failures.
  </done>
</task>

</tasks>

<verification>
1. `npm run compile` succeeds with no errors
2. `npm run typecheck` passes
3. `npm run lint` passes
4. Grep `writeJsonFile` — every call site has error handling upstream
5. Grep `showWriteError` — used in all command try-catch blocks
6. Grep `setPluginEnabled` in extension.ts — wrapped in try-catch
7. No `showErrorMessage.*Failed to` patterns remain in command files (replaced by showWriteError)
</verification>

<success_criteria>
- All configWriter function callers handle write errors with user-facing notifications
- Error messages include scope label and file path (e.g., "Failed to write User settings (~/.claude/settings.json): EACCES: permission denied")
- "Open File" button opens the config file in the editor
- "Retry" button re-attempts the write operation
- Plugin checkbox toggle failures show error notification
- No unhandled promise rejections from write operations
- TypeScript compiles cleanly, lint passes
</success_criteria>

<output>
After completion, create `.planning/phases/10-error-handling-foundation/10-01-SUMMARY.md`
</output>
