---
phase: 02-remove-refresh-toolbar-button
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [package.json]
autonomous: true
requirements: [REFR-01, REFR-02, REFR-03]

must_haves:
  truths:
    - "TreeView toolbar no longer displays a Refresh icon button"
    - "claudeConfig.refresh appears and is functional in the VS Code Command Palette"
    - "External edits to config files still auto-refresh the tree via file watcher"
  artifacts:
    - path: "package.json"
      provides: "Extension manifest with refresh toolbar entry removed"
      contains: "claudeConfig.refresh"
  key_links:
    - from: "src/extension.ts"
      to: "configStore.reload()"
      via: "registerCommand('claudeConfig.refresh')"
      pattern: "registerCommand\\('claudeConfig\\.refresh'"
    - from: "src/watchers/fileWatcher.ts"
      to: "configStore.reload()"
      via: "debouncedReload()"
      pattern: "configStore\\.reload\\(\\)"
---

<objective>
Remove the Refresh toolbar button from the TreeView while preserving Command Palette access and programmatic functionality.

Purpose: The Refresh toolbar button is redundant — `ConfigFileWatcher` auto-refreshes on any config file change. Removing it declutters the toolbar. The command itself must remain accessible for power users (Command Palette) and for programmatic `executeCommand` calls.

Output: Updated `package.json` with the `view/title` menu entry for `claudeConfig.refresh` deleted. No other files change.
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remove-refresh-toolbar-button/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove refresh toolbar button entry from package.json</name>
  <files>package.json</files>
  <action>
In `package.json`, locate the `contributes.menus["view/title"]` array. Find and delete the object:

```json
{
  "command": "claudeConfig.refresh",
  "when": "view == claudeConfigTree",
  "group": "navigation@99"
}
```

This is currently at approximately lines 172-176 of package.json. Also remove any trailing comma that would create invalid JSON after the deletion.

**Do NOT modify any other part of package.json.** Specifically:

1. **KEEP** the `contributes.commands` entry for `claudeConfig.refresh` (approx. lines 48-53). This entry is required for the command to appear in the VS Code Command Palette (REFR-03). Removing it would silently remove the command from the palette.

2. **KEEP** the `"icon": "$(refresh)"` field in the `contributes.commands` entry. It is harmless without a toolbar button and removing it is unnecessary churn.

3. **Do NOT touch `src/extension.ts`**. The `registerCommand('claudeConfig.refresh', ...)` call and its `refreshCmd` in `context.subscriptions.push(...)` must remain. This satisfies REFR-03 (programmatic access preserved). REFR-02 as written conflicts with REFR-03 and the ROADMAP; the ROADMAP explicitly says "Verify command registration in extension.ts is retained (REFR-03)." Treat REFR-02 as satisfied by the package.json toolbar entry removal.

**Requirement mapping:**
- REFR-01: Satisfied by removing the `view/title` menu entry (the toolbar button). The "command entry" in REFR-01 refers to this menu entry, not the `contributes.commands` entry — per research finding and REFR-03 constraint.
- REFR-02: Satisfied by the removal of the toolbar entry from package.json. The extension.ts `registerCommand` is intentionally preserved per REFR-03 and the ROADMAP goal statement.
- REFR-03: Satisfied by keeping both the `contributes.commands` entry in package.json (Command Palette visibility) and the `registerCommand` call in extension.ts (runtime handler).
  </action>
  <verify>
Run these three verification checks:

1. `grep '"command": "claudeConfig.refresh"' package.json` — must return exactly ONE match (the `contributes.commands` entry only). Two matches means the `view/title` entry was not removed.

2. `grep 'navigation@99' package.json` — must return ZERO matches. Any match means the toolbar button entry still exists.

3. `grep 'claudeConfig.refresh' src/extension.ts` — must return at least one match (the `registerCommand` line). Zero matches would mean extension.ts was incorrectly modified.

4. `npm run typecheck` — must pass. No TypeScript changes were made, but validate that package.json is valid JSON.

5. `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` — must exit 0 (valid JSON after editing).
  </verify>
  <done>
The `view/title` array in package.json no longer contains a `claudeConfig.refresh` entry. The `contributes.commands` array still contains the `claudeConfig.refresh` entry with title, category, and icon. The extension.ts `registerCommand` call is untouched. The command will appear in Command Palette but not on the TreeView toolbar.
  </done>
</task>

</tasks>

<verification>
1. **Toolbar button removed (REFR-01):** `grep 'navigation@99' package.json` returns 0 matches.
2. **Command Palette preserved (REFR-03):** `grep '"command": "claudeConfig.refresh"' package.json` returns exactly 1 match (in `contributes.commands`).
3. **Runtime handler preserved (REFR-03):** `grep "registerCommand.*claudeConfig.refresh" src/extension.ts` returns 1 match.
4. **File watcher unaffected:** `grep "configStore.reload" src/watchers/fileWatcher.ts` returns at least 1 match (no changes to watcher).
5. **Valid JSON:** `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` exits 0.
6. **TypeScript compiles:** `npm run typecheck` passes.
</verification>

<success_criteria>
1. The TreeView toolbar no longer displays a Refresh icon button.
2. `claudeConfig.refresh` is still accessible and functional via the VS Code Command Palette.
3. External edits to any config file still auto-refresh the tree within the file watcher debounce interval.
</success_criteria>

<output>
After completion, create `.planning/phases/02-remove-refresh-toolbar-button/02-01-SUMMARY.md`
</output>
