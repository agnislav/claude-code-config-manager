---
phase: 01-quickpick-multi-select-filter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/extension.ts
  - src/tree/configTreeProvider.ts
  - package.json
autonomous: true
requirements:
  - FILT-07
  - FILT-08

must_haves:
  truths:
    - "No filter icon buttons appear in the TreeView toolbar (only Refresh remains)"
    - "No claudeConfig_filter_* context keys are set on activation"
    - "The tree still renders all sections when no filter is active (empty _sectionFilter set)"
  artifacts:
    - path: "src/extension.ts"
      provides: "Extension entry point without filter command registrations"
      contains: "refreshCmd"
    - path: "src/tree/configTreeProvider.ts"
      provides: "Tree provider without syncFilterContext or FILTER_CTX_KEYS"
    - path: "package.json"
      provides: "Manifest without filter commands or filter toolbar entries"
  key_links:
    - from: "src/extension.ts"
      to: "src/tree/configTreeProvider.ts"
      via: "treeProvider._sectionFilter still exists and drives section visibility"
      pattern: "_sectionFilter"
---

<objective>
Remove all old filter infrastructure — the 8 toolbar icon buttons, their 16 command registrations
(inactive + active variants), and the `syncFilterContext()` / `FILTER_CTX_KEYS` machinery that drove
icon-swap via `setContext`. The internal `_sectionFilter` Set on `ConfigTreeProvider` is preserved;
only the external trigger mechanism (toolbar icons + commands + context keys) is deleted.

Purpose: Clear the way for Plan 01-02 to add the single QuickPick filter command without conflicts
or orphaned registrations.

Output: Three modified files (`extension.ts`, `configTreeProvider.ts`, `package.json`) with all
filter-related commands, context keys, and menu entries removed.
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/extension.ts
@src/tree/configTreeProvider.ts
@package.json
@src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove filter command registrations from extension.ts</name>
  <files>src/extension.ts</files>
  <action>
In `src/extension.ts`, delete the following code blocks:

1. **Lines 48-63** — The `filterAllCmd` registration, `filterAllActiveCmd` registration, and the
   entire `for (const st of Object.values(SectionType))` loop that registers
   `claudeConfig.filter.${st}` and `claudeConfig.filter.${st}.active` commands.

2. **Line 15** — Remove the `SectionType` import from the `import { SectionType } from './types'`
   line. (If other imports remain on that line like `findKeyPathAtLine`, keep those.)

3. **Line 160** — In the `context.subscriptions.push(...)` call, remove `filterAllCmd` and
   `filterAllActiveCmd` from the argument list. The `refreshCmd` and all other disposables stay.

After removal, the only filter-related code remaining should be:
- `_sectionFilter` Set in `configTreeProvider.ts` (preserved — Plan 01-02 will use it)
- `treeProvider.toggleSectionFilter()` and `treeProvider.selectAllSections()` methods (preserved —
  Plan 01-02 will call them from the new QuickPick command)

Do NOT remove the `toggleSectionFilter` or `selectAllSections` methods from `ConfigTreeProvider` —
they are still needed.
  </action>
  <verify>
Run `npm run typecheck` — must pass with zero errors. The removed commands should not be referenced
anywhere else in the codebase. Verify with: `grep -r "filterAll\|filter\.permissions\|filter\.sandbox\|filter\.hooks\|filter\.mcpServers\|filter\.env\|filter\.plugins\|filter\.settings" src/ --include="*.ts"` — should return zero matches in `extension.ts` (matches in `configTreeProvider.ts` for `_sectionFilter` are expected and correct).
  </verify>
  <done>
`extension.ts` no longer registers any `claudeConfig.filterAll`, `claudeConfig.filterAll.active`,
`claudeConfig.filter.{section}`, or `claudeConfig.filter.{section}.active` commands. The
`SectionType` import is removed from this file. The subscriptions array no longer includes
`filterAllCmd` or `filterAllActiveCmd`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove syncFilterContext and FILTER_CTX_KEYS from configTreeProvider.ts</name>
  <files>src/tree/configTreeProvider.ts</files>
  <action>
In `src/tree/configTreeProvider.ts`, delete the following:

1. **The `FILTER_CTX_KEYS` static property** (lines 44-52) — the entire
   `private static readonly FILTER_CTX_KEYS: Record<SectionType, string>` object.

2. **The `syncFilterContext()` method** (lines 54-65) — the entire private method that calls
   `vscode.commands.executeCommand('setContext', ...)` for `claudeConfig_filter_all` and each
   `claudeConfig_filter_${ctxKey}`.

3. **All calls to `this.syncFilterContext()`** — there are exactly three call sites:
   - In the constructor (line 20): `this.syncFilterContext();`
   - In `toggleSectionFilter()` (line 33): `this.syncFilterContext();`
   - In `selectAllSections()` (line 39): `this.syncFilterContext();`

   Remove all three calls. The `toggleSectionFilter()` and `selectAllSections()` methods themselves
   must remain — only the `syncFilterContext()` calls inside them are removed.

After removal, the `ConfigTreeProvider` class should still have:
- `_sectionFilter` private field (Set<SectionType>)
- `sectionFilter` getter (ReadonlySet<SectionType>)
- `toggleSectionFilter(section)` method (now just toggles the set + calls `this.refresh()`)
- `selectAllSections()` method (now just clears the set + calls `this.refresh()`)
  </action>
  <verify>
Run `npm run typecheck` — must pass with zero errors. Verify no references to `syncFilterContext`
or `FILTER_CTX_KEYS` remain: `grep -r "syncFilterContext\|FILTER_CTX_KEYS" src/ --include="*.ts"`
should return zero matches.
  </verify>
  <done>
`configTreeProvider.ts` no longer contains `syncFilterContext()`, `FILTER_CTX_KEYS`, or any
`setContext` calls for filter context keys. The `_sectionFilter` Set and its public API
(`sectionFilter`, `toggleSectionFilter`, `selectAllSections`) are preserved for Plan 01-02.
  </done>
</task>

<task type="auto">
  <name>Task 3: Remove filter commands and toolbar entries from package.json</name>
  <files>package.json</files>
  <action>
In `package.json`, remove all filter-related entries from three sections:

**1. `contributes.commands` array** — Remove these 16 command objects (8 inactive + 8 active):
- `claudeConfig.filterAll` and `claudeConfig.filterAll.active`
- `claudeConfig.filter.permissions` and `claudeConfig.filter.permissions.active`
- `claudeConfig.filter.sandbox` and `claudeConfig.filter.sandbox.active`
- `claudeConfig.filter.hooks` and `claudeConfig.filter.hooks.active`
- `claudeConfig.filter.mcpServers` and `claudeConfig.filter.mcpServers.active`
- `claudeConfig.filter.env` and `claudeConfig.filter.env.active`
- `claudeConfig.filter.plugins` and `claudeConfig.filter.plugins.active`
- `claudeConfig.filter.settings` and `claudeConfig.filter.settings.active`

Keep ALL other commands (refresh, openFile, add*, edit*, delete*, move*, toggle*, create*, reveal*,
copy*, openPluginReadme).

**2. `contributes.menus.view/title` array** — Remove all 16 filter toolbar entries (the objects
whose `command` starts with `claudeConfig.filter`). That means:
- Remove `claudeConfig.filterAll` (navigation@0, when `!claudeConfig_filter_all`)
- Remove `claudeConfig.filterAll.active` (navigation@0, when `claudeConfig_filter_all`)
- Remove all 14 section filter entries (navigation@1 through navigation@7, both active/inactive)

Keep the `claudeConfig.refresh` entry (navigation@99).

**3. `contributes.menus.commandPalette` array** — Remove all 16 filter command palette entries:
- `claudeConfig.filterAll` through `claudeConfig.filter.settings.active` (all with `"when": "false"`)

Keep all other commandPalette entries (editValue, deleteItem, moveToScope, etc.).

After removal, verify the JSON is valid (no trailing commas, correct array brackets).
  </action>
  <verify>
Run `node -e "JSON.parse(require('fs').readFileSync('package.json','utf8'))"` to verify JSON
validity. Then run `npm run typecheck` to confirm no manifest/code mismatch. Verify with:
`grep "claudeConfig.filter" package.json` — should return zero matches.
  </verify>
  <done>
`package.json` contains zero filter-related command definitions, zero filter toolbar menu entries,
and zero filter commandPalette entries. The `claudeConfig.refresh` toolbar button and all non-filter
commands remain intact. JSON is syntactically valid.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with zero errors
2. `npm run compile` succeeds (esbuild bundle builds)
3. `grep -r "syncFilterContext\|FILTER_CTX_KEYS\|claudeConfig_filter_\|filterAllCmd\|filterAllActiveCmd" src/ --include="*.ts"` returns zero matches
4. `grep "claudeConfig.filter" package.json` returns zero matches
5. `grep "claudeConfig.refresh" package.json` returns matches (refresh button preserved)
6. The tree still builds correctly (ConfigTreeProvider._sectionFilter exists and is empty by default → all sections shown)
</verification>

<success_criteria>
All 16 filter commands removed from package.json. All filter toolbar icons removed. All setContext
filter machinery removed from configTreeProvider.ts. All filter command registrations removed from
extension.ts. TypeScript compiles without errors. The _sectionFilter Set and its methods are
preserved for Plan 01-02 to wire to the new QuickPick command.
</success_criteria>

<output>
After completion, create `.planning/phases/01-quickpick-multi-select-filter/01-01-SUMMARY.md`
</output>
