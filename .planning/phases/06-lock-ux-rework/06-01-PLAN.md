---
phase: 06-lock-ux-rework
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/extension.ts
  - package.json
autonomous: true
requirements:
  - LOCK-11
  - LOCK-12
  - LOCK-13

must_haves:
  truths:
    - "On extension activation, the User scope is locked — write operations are blocked before the user touches anything"
    - "When the User scope is locked, the toolbar shows $(lock) icon; when unlocked, it shows $(unlock) icon"
    - "The lock toggle button appears in the toolbar (view/title), not on the User scope node inline"
    - "The lock toggle button is positioned left of the filter button in the toolbar"
    - "Clicking the toolbar lock button toggles the lock state and updates the icon atomically with no reload required"
  artifacts:
    - path: "src/extension.ts"
      provides: "Lock-by-default activation, lock commands"
      contains: "configStore.lockScope(ConfigScope.User)"
    - path: "package.json"
      provides: "Toolbar menu entries for lock toggle"
      contains: "view/title"
  key_links:
    - from: "src/extension.ts"
      to: "package.json"
      via: "setContext claudeConfig_userScope_locked context key drives when clauses"
      pattern: "claudeConfig_userScope_locked"
    - from: "package.json view/title lock entries"
      to: "package.json view/item/context"
      via: "lock entries removed from inline, added to toolbar"
      pattern: "lockUserScope|unlockUserScope"
---

<objective>
Lock User scope by default on activation, ensure correct icon semantics, and move the lock toggle from the User scope inline button to the toolbar.

Purpose: Users get lock-by-default protection (LOCK-11), correct icon communication (LOCK-12), and a discoverable toolbar control (LOCK-13) — the lock button is always visible regardless of which tree node is selected.

Output: Updated `extension.ts` with lock-by-default activation. Updated `package.json` with lock toggle in view/title toolbar and inline lock buttons removed.
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/extension.ts
@package.json
@src/config/configModel.ts
@src/tree/configTreeProvider.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock User scope by default on activation</name>
  <files>src/extension.ts</files>
  <action>
In `src/extension.ts`, change the lock initialization logic so the User scope is locked by default on activation:

1. **Lock the scope in ConfigStore before first tree render.** After `configStore.reload()` (line ~27), add:
   ```ts
   configStore.lockScope(ConfigScope.User);
   ```

2. **Set the context key to `true` instead of `false`.** Change line ~29 from:
   ```ts
   vscode.commands.executeCommand('setContext', 'claudeConfig_userScope_locked', false);
   ```
   to:
   ```ts
   vscode.commands.executeCommand('setContext', 'claudeConfig_userScope_locked', true);
   ```

The order must be: `configStore.reload()` → `configStore.lockScope(ConfigScope.User)` → `setContext(... true)` → create tree provider. This ensures the tree's first render sees the locked state.
  </action>
  <verify>
Run `npm run typecheck` — should pass with no errors. Verify by reading the file that:
1. `configStore.lockScope(ConfigScope.User)` appears after `configStore.reload()` and before the tree provider constructor
2. `setContext` call uses `true` as the initial value
  </verify>
  <done>
On extension activation, User scope is locked before the first tree render. The context key `claudeConfig_userScope_locked` starts as `true`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap lock icons to state semantics, move toggle to toolbar, remove inline entries</name>
  <files>package.json</files>
  <action>
In `package.json`, make four changes:

**1. Swap icon assignments on command definitions (LOCK-12).** The icons should reflect the current state, not the action. Change the `commands` array entries:

- `claudeConfig.lockUserScope`: change `"icon": "$(lock)"` → `"icon": "$(unlock)"`
  (shown when unlocked — icon communicates "this is unlocked")
- `claudeConfig.unlockUserScope`: change `"icon": "$(unlock)"` → `"icon": "$(lock)"`
  (shown when locked — icon communicates "this is locked")

**2. Add lock toggle entries to `view/title` array.** Add two new entries BEFORE the existing filter entries so the lock button appears left of filter. Use `navigation@-1` group to position before filter's `navigation@0`:

```json
{
  "command": "claudeConfig.lockUserScope",
  "when": "view == claudeConfigTree && !claudeConfig_userScope_locked",
  "group": "navigation@-1"
},
{
  "command": "claudeConfig.unlockUserScope",
  "when": "view == claudeConfigTree && claudeConfig_userScope_locked",
  "group": "navigation@-1"
}
```

These use the same dual-entry icon-swap pattern already used for filter/filter-active. The `@-1` sort order ensures lock appears left of filter (`@0`).

**3. Remove lock entries from `view/item/context` array.** Delete these two objects completely (currently at roughly lines 192-201):
```json
{
  "command": "claudeConfig.lockUserScope",
  "when": "view == claudeConfigTree && viewItem =~ /^scope\\.user/ && !claudeConfig_userScope_locked",
  "group": "inline@0"
},
{
  "command": "claudeConfig.unlockUserScope",
  "when": "view == claudeConfigTree && viewItem =~ /^scope\\.user/ && claudeConfig_userScope_locked",
  "group": "inline@0"
}
```

**4. No changes to the `commandPalette` deny-list.** `lockUserScope` and `unlockUserScope` remain hidden from Command Palette. `toggleUserLock` remains the only palette-accessible lock command.

After this change, the `view/title` array should have 4 entries:
1. `claudeConfig.lockUserScope` (navigation@-1, when !locked) — shows $(unlock) icon (state: unlocked)
2. `claudeConfig.unlockUserScope` (navigation@-1, when locked) — shows $(lock) icon (state: locked)
3. `claudeConfig.filterSections` (navigation@0, when !filterActive)
4. `claudeConfig.filterSections.active` (navigation@0, when filterActive)
  </action>
  <verify>
Run `npm run typecheck` — should pass. Read `package.json` and verify:
1. Command `claudeConfig.lockUserScope` has `"icon": "$(unlock)"` (swapped)
2. Command `claudeConfig.unlockUserScope` has `"icon": "$(lock)"` (swapped)
3. `view/title` has exactly 4 entries: 2 lock + 2 filter
4. `view/item/context` has NO entries referencing `lockUserScope` or `unlockUserScope`
5. Lock entries use `group: "navigation@-1"` and filter entries use `group: "navigation@0"`
6. `commandPalette` still has `lockUserScope` and `unlockUserScope` with `when: false`
  </verify>
  <done>
Lock icons use state semantics: $(lock) when locked, $(unlock) when unlocked. Lock toggle button appears in toolbar (view/title) left of filter. No lock buttons on scope node inline.
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes with no errors
2. `npm run compile` succeeds
3. In `extension.ts`: `configStore.lockScope(ConfigScope.User)` is called between `reload()` and tree provider creation
4. In `extension.ts`: `setContext('claudeConfig_userScope_locked', true)` sets locked by default
5. In `package.json` `view/title`: lock toggle entries exist with `navigation@-1` group
6. In `package.json` `view/title`: filter entries exist with `navigation@0` group (unchanged)
7. In `package.json` `view/item/context`: no `lockUserScope` or `unlockUserScope` entries remain
8. In `package.json` `commandPalette`: `lockUserScope` and `unlockUserScope` remain hidden (when: false)
</verification>

<success_criteria>
- Extension activates with User scope locked by default — blocked before user interaction
- Toolbar shows $(lock) when locked, $(unlock) when unlocked — state semantics
- Lock toggle is in the toolbar, positioned left of the filter button
- Clicking the toolbar lock button toggles state and swaps icon without reload
- No lock buttons appear on the User scope tree node inline
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-lock-ux-rework/06-01-SUMMARY.md`
</output>
