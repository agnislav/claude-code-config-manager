---
phase: 18-verification-and-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/runTests.ts
  - test/suite/index.ts
  - test/suite/viewmodel/builder.test.ts
autonomous: true
requirements: [VM-11, VM-12]

must_haves:
  truths:
    - "npm run test executes the Mocha test runner inside VS Code Extension Host"
    - "Test scaffold discovers .test.js files automatically via glob"
    - "Zero overrideResolver imports exist in src/tree/nodes/"
    - "baseNode.ts has no ScopedConfig-dependent logic"
  artifacts:
    - path: "test/runTests.ts"
      provides: "@vscode/test-electron launcher"
      min_lines: 10
    - path: "test/suite/index.ts"
      provides: "Mocha runner with glob-based test discovery"
      min_lines: 15
    - path: "test/suite/viewmodel/builder.test.ts"
      provides: "Test helpers and placeholder suite for Plan 02"
      min_lines: 40
  key_links:
    - from: "test/runTests.ts"
      to: "test/suite/index.ts"
      via: "extensionTestsPath resolve"
      pattern: "resolve.*suite/index"
    - from: "test/suite/index.ts"
      to: "test/suite/viewmodel/builder.test.ts"
      via: "glob discovery of .test.js files"
      pattern: "glob.*\\.test\\.js"
---

<objective>
Create the VS Code extension test infrastructure and verify cleanup requirements VM-11 and VM-12.

Purpose: Phase 18 needs test infrastructure before builder tests can run. The project currently has zero test files despite having test dependencies in package.json. Also verify that the decoupling cleanup (no overrideResolver imports in nodes, simplified baseNode) is complete.

Output: Working test scaffold (runTests.ts, suite/index.ts), test helper utilities and initial test file with cleanup verification assertions, `npm run test` executes successfully.
</objective>

<execution_context>
@/Users/agnislav/.claude/get-shit-done/workflows/execute-plan.md
@/Users/agnislav/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-verification-and-cleanup/18-RESEARCH.md

@src/viewmodel/types.ts
@src/viewmodel/builder.ts
@src/types.ts
@src/config/configModel.ts
@tsconfig.test.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test infrastructure scaffold</name>
  <files>test/runTests.ts, test/suite/index.ts</files>
  <action>
Create the standard VS Code extension test scaffold:

1. Create `test/runTests.ts` -- the @vscode/test-electron launcher:
   - Import `path` and `runTests` from `@vscode/test-electron`
   - Set `extensionDevelopmentPath` to `path.resolve(__dirname, '../../')` (project root)
   - Set `extensionTestsPath` to `path.resolve(__dirname, './suite/index')`
   - Call `runTests({ extensionDevelopmentPath, extensionTestsPath })`
   - Catch errors, log them, and `process.exit(1)`

2. Create `test/suite/index.ts` -- the Mocha runner:
   - Import `path`, `Mocha` (default import), and `glob` from `glob`
   - Create and export `run()` function returning `Promise<void>`
   - Configure Mocha: `{ ui: 'bdd', color: true, timeout: 10000 }`
   - Use `glob('**/**.test.js', { cwd: testsRoot })` for test discovery
   - Add each found file to Mocha, then run
   - Reject on failures, resolve on success

Follow the exact patterns from the research document code examples. Use `import Mocha from 'mocha'` (default import) and `import { glob } from 'glob'` (named import).
  </action>
  <verify>
    <automated>cd /Users/agnislav/Projects/Dardes/claude-code-config-manager && npx tsc -p tsconfig.test.json --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>test/runTests.ts and test/suite/index.ts exist, TypeScript compiles without errors using tsconfig.test.json</done>
</task>

<task type="auto">
  <name>Task 2: Create test helpers and cleanup verification tests</name>
  <files>test/suite/viewmodel/builder.test.ts</files>
  <action>
Create `test/suite/viewmodel/builder.test.ts` with:

1. **Imports:** `assert` (node built-in), `TreeViewModelBuilder` from `../../../src/viewmodel/builder`, `ConfigScope`, `ScopedConfig`, `ClaudeCodeConfig`, `NodeContext` from `../../../src/types`, `NodeKind`, `BaseVM` from `../../../src/viewmodel/types`, `ConfigStore` from `../../../src/config/configModel`.

2. **Test helper: createMockConfigStore(scopedConfigs, options?)** -- Returns a minimal ConfigStore stub with:
   - `getWorkspaceFolderKeys()` returns `['__global__']`
   - `getAllScopes()` returns the provided scopedConfigs
   - `isMultiRoot()` returns `options?.multiRoot ?? false`
   - `isScopeLocked(scope)` returns `options?.lockedScopes?.includes(scope) ?? false`
   - `getDiscoveredPaths()` returns `undefined`
   - Cast as `unknown as ConfigStore`

3. **Test helper: makeScopedConfig(scope, config, overrides?)** -- Creates ScopedConfig with:
   - `scope` from parameter
   - `filePath` as `/mock/${scope}/settings.json`
   - `fileExists: true`
   - `config` cast as `ClaudeCodeConfig`
   - `isReadOnly: scope === ConfigScope.Managed`
   - Spread `overrides` last

4. **Test helper: findVM(vms, kind, label?)** -- Recursive depth-first search through vm.children tree, returns first match by kind (and optional label).

5. **Test helper: findAllVMs(vms, kind)** -- Recursive collector, returns all VMs matching given kind.

6. **Cleanup verification suite** (`suite('Cleanup Verification (VM-11, VM-12)')`) with two tests:
   - `test('VM-11: no overrideResolver imports in src/tree/nodes/')` -- Use `fs.readdirSync` to list all `.ts` files in `src/tree/nodes/`, read each with `fs.readFileSync`, assert none contain `'overrideResolver'` string. Use `path.resolve(__dirname, '../../../src/tree/nodes')` for the directory path (from compiled `out/test/suite/viewmodel/`).
   - `test('VM-12: baseNode has no ScopedConfig-dependent logic')` -- Read `src/tree/nodes/baseNode.ts` content, assert it does NOT contain `'ScopedConfig'`, assert it DOES contain `'BaseVM'` (confirming it uses VM pattern).

7. **Smoke test suite** (`suite('TreeViewModelBuilder - Smoke')`) with one test:
   - `test('builds scope VMs from single-scope config')` -- Create a single User-scope ScopedConfig with `{ model: 'claude-3' }`, build VMs, assert result is an array with length 1, assert first VM has `kind === NodeKind.Scope`.

This establishes the test file structure and helpers that Plan 02 will extend with comprehensive entity type tests.
  </action>
  <verify>
    <automated>cd /Users/agnislav/Projects/Dardes/claude-code-config-manager && npm run test 2>&1 | tail -20</automated>
  </verify>
  <done>npm run test passes with cleanup verification tests (VM-11, VM-12) and smoke test green. Test helpers (createMockConfigStore, makeScopedConfig, findVM, findAllVMs) are available for Plan 02.</done>
</task>

</tasks>

<verification>
- `npm run test` exits with code 0
- All 3 tests pass (VM-11 cleanup check, VM-12 cleanup check, builder smoke test)
- `test/runTests.ts`, `test/suite/index.ts`, `test/suite/viewmodel/builder.test.ts` all exist
- TypeScript compiles cleanly with `tsconfig.test.json`
</verification>

<success_criteria>
- Test infrastructure is functional and discovers tests automatically
- VM-11 verified: zero overrideResolver imports in src/tree/nodes/
- VM-12 verified: baseNode.ts uses BaseVM, not ScopedConfig
- Smoke test confirms TreeViewModelBuilder produces ScopeVM from basic input
- All helpers ready for Plan 02 to add comprehensive tests
</success_criteria>

<output>
After completion, create `.planning/phases/18-verification-and-cleanup/18-01-SUMMARY.md`
</output>
